.venv/
__pycache__/
*.pyc
.pytest_cache/

web/node_modules/
web/dist/
*.tsbuildinfo

# macOS/Finder 가 만든 중복 사본 ("generator 2.py" 같은 것)
* 2.*

# 생성 산출물 — 운영에서는 CI가 매 배포마다 다시 만든다.
# 문서를 리뷰 대상으로 삼고 싶으면 아래 두 줄을 주석 처리하고 커밋한다.
docs/*.md
docs/index.json

# 뷰어에서 불러온 로컬 프로젝트들 (사용자 머신마다 다름)
projects/

.DS_Store
