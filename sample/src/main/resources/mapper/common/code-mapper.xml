<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gng.common.code.CodeMapper">

  <select id="selectCodeList" resultType="java.util.HashMap">
    SELECT CD, CD_NM, SORT_ORD
    FROM TB_COM_CODE
    WHERE CD_GRP = #{cdGrp}
      AND USE_YN = 'Y'
    ORDER BY SORT_ORD
  </select>

  <select id="selectCodeName" resultType="string">
    SELECT CD_NM
    FROM TB_COM_CODE
    WHERE CD_GRP = #{cdGrp}
      AND CD = #{cd}
  </select>

</mapper>
